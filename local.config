singularity {
        enabled = true
        runOptions = '-B /b:/b -B /bscratch:/bscratch -B /bscratch/vishnu/BEAMFORMER/:/workspace/BEAMFORMER'
        envWhitelist = 'SINGULARITY_BINDPATH, SINGULARITY_LD_LIBRARY_PATH'
    }

// Run Locally

    process {
        executor = 'local'
    }