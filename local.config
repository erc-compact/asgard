 apptainer {
        enabled = true
        runOptions = '--nv -H $HOME:/home1 -B /b/u/vishnu/BEAMFORMER/:/workspace/BEAMFORMER'
        //envWhitelist = 'APPTAINER_BINDPATH, APPTAINER_LD_LIBRARY_PATH'
    }

// Run Locally

    process {
        executor = 'local'
    }
